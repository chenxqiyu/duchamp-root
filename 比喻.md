# 幼儿园闯关游戏 —— duchamp-root 提权全流程比喻

> 本文档把整个项目（针对小米 HyperOS 3.0 / 内核 6.1.138 的一键 Root 工具）
> 用"幼儿园闯关游戏"的比喻讲清楚。每个比喻都对应真实的技术实现，
> 代码里已按本比喻添加了中文注释（`【】`标记）。

---

## 🏫 整体设定

| 比喻 | 真实含义 |
|---|---|
| 园长 | 手机内核（最高权威，管着所有小朋友和玩具） |
| 小朋友 | 普通 App（只能玩自己的玩具，不能碰别人的） |
| 园长助手 | Root 权限（可以打开所有柜子、修改所有规则、给任何人发糖果） |
| 门锁缺陷 | CVE-2026-43499（`rt_mutex` "鬼标签"漏洞） |
| 魔法道具 | `preload.so`（偷偷塞进小朋友书包里的道具，LD_PRELOAD 加载） |
| 魔法书包 | 被回收并受控的内核页（KernelSnitch 页回收 + skb 喷射） |
| 全园警报 | 内核 panic → 手机重启 |

**核心漏洞一句话**：该撕 A 的标签，错撕了 B 的；A 被移走后标签还在，
变成了可以被人利用的"鬼标签"。
（认错对象 → 标签残留 → 被恶意利用）

---

## 🎮 五关闯关比喻

### 第 1 关：偷看园长的排班表（KASLR 泄漏）

**技术**：`pselect` slide 侧信道 + 鬼标签写 → 读 `boot_id` 数据别名 → 推出 `_stext`

**比喻**：
园长每天换办公室位置（KASLR），你不知道她在哪。但你发现她有个习惯：
每次让小朋友传话时，会在纸条背面不小心印出办公室的门牌号。
你故意让她多传几次话（`pselect` 反复调用），从纸条背面的墨痕里
拼出了完整的门牌号（`_stext` 地址）。

**通关奖励**：知道了园长办公室在哪。

**本关内部小步骤**（代码 `targets/duchamp/slide.c`）：

| 小步骤 | 比喻 | 状态 |
|---|---|---|
| 1. 触发传话漏洞 | waiter/owner 演双簧形成死锁环，`FUTEX_CMP_REQUEUE_PI` 走进 buggy 回滚，鬼标签诞生 | ✅ |
| 2. 制作假纸条 | `pselect` 栈覆盖，fd_set 把伪造的 `rt_mutex_waiter` 写到内核栈 | ✅ |
| 3. 准备魔法书包 | KernelSnitch 页回收，假结构体落在可控内核页 | ✅ |
| 4. 让老师改名字不被发现 | consumer `sched_setattr` 触发 PI 链遍历 | ⚠️ 卡点（见下） |
| 5. 拼出真门牌号 | 读 `boot_id` 减偏移得 `_stext` | ⏸️ 待 4 通过后 |

**第 4 步卡点与修复**：
老师改名字时（NICE-ENTER），园长查纸条上的"鬼标签"挂错了颜色——
假标签在红黑树里被标成黑色，摘标签时园长要找"邻居标签"核对，
结果邻居不存在（`rb_erase` → `__rb_erase_color` 空指针解引用）→ 全园警报。

修复（三件套，已编译待上机验证）：
1. 所有假标签父指针染**红**（RED）—— 摘红色叶子标签不用找邻居，零副作用
2. 假标签树根 `fake_w0` 染**黑**（BLACK）—— 补挂标签时不会触发"红红冲突"找祖父
3. 整张假标签图**页内自包含** —— 所有指针都指向魔法书包内部，绝不碰随机化后的未映射地址

---

### 第 2 关：偷换玩具柜的钥匙牌（FOPS 劫持）

**技术**：`pselect` 任意栈写 + `FUTEX_CMP_REQUEUE_PI` 竞争条件改 ops 表

**比喻**：
幼儿园有个玩具柜，上面挂着"谁能开这个柜子"的钥匙牌（文件操作 ops 表）。
你趁两个老师交接钥匙的瞬间（futex PI 竞争条件），把钥匙牌上的名字
涂改成你自己的名字。因为动作太快，没人注意到。

具体手法（代码 `fops.c`）：
- 假排班表里把"鬼标签的 PI 树父指针"指向钥匙牌上要涂改的那一格
- 园长整理鬼标签时，顺手把书包地址写到那一格
- 以后对 ashmem 调 `llseek/ioctl/read/write`，园长按图索骥拿到的是
  书包里的假钥匙牌 → 假函数指针 → 想干嘛就干嘛

**通关奖励**：现在你可以假装是"被授权开柜子的人"（任意地址写 8 字节）。

---

### 第 3 关：拿到万能钥匙（物理读写原语）

**技术**：伪造 `pipe_buf_ops` → 任意内核物理地址读写

**比喻**：
你用偷换来的钥匙牌打开了一个特殊柜子，里面有一根"魔法水管"
（pipe buffer）。这根水管本来只能通水，但你把它改造成了
"万能钥匙模具"——你想让它变成什么形状，它就变成什么形状。

改造方法（代码 `pipe.c`）：
- 水管的"阀门"（`pipe_buffer.ops`）换成书包里伪造的假阀门表
- `pipe_buffer.page` 指向想读/写的物理页
- 之后 `read(pipe)` = 园长帮你读任意柜子；`write(pipe)` = 写任意柜子

**通关奖励**：拥有了修改任何东西的能力（全内核内存读写）。

---

### 第 4 关：给自己戴上园长徽章（提权）

**技术**：patch cred（uid=0，cap 全满）+ 关 seccomp + selinux enforce=0

**比喻**：
你用万能钥匙打开园长的人事档案柜，找到自己的名字，
把身份改成"园长助手"，把所有权限印章都盖满。
然后顺手把门口的"安全检查员"（seccomp）和"纪律监督员"（SELinux）
都请去喝茶了——现在没人会质疑你的身份。

具体步骤（代码 `root.c`）：
1. 沿花名册（`init_task` 任务链表）逐个翻档案，按名字（"ll_root_child"）找到我们的小朋友
2. 把 `task_struct` 里 `cred/real_cred` 两个指针指向"满权限"假徽章
3. 换 SELinux 语境（security blob → kernel sid）
4. 清 seccomp（TIF_SECCOMP / PFA_NO_NEW_PRIVS / mode / filter）
5. 小朋友自验 `setgid(0)/setuid(0)` 全过 = 徽章生效

**通关奖励**：正式成为园长的助手，拥有 root 权限。

---

### 第 5 关：安插自己的管家（持久化 KernelSU）

**技术**：嵌入 ksud 二进制到 `preload.so`，启动 KernelSU 守护进程

**比喻**：
你怕下次进来还要重新闯关，于是把一个忠诚的小机器人（ksud）
藏在书包里带进来。这个小机器人会自动在幼儿园里安家，
以后其他小朋友想当助手时，直接找它盖章就行，不用再闯五关了。

具体步骤（代码 `preload.c`）：
1. 把小机器人写到 `/data/local/tmp/ksud`（先写临时名再 rename，原子落位）
2. 盖好章（`chown 0:0` / `chmod 755` / `chcon` 语境）
3. 派它上岗（`ksud --daemon` 后台常驻）

**通关奖励**：Root 权限永久生效，且可管理。

---

## 📁 代码文件 × 比喻对照表

| 文件 | 比喻角色 | 职责 |
|---|---|---|
| `src/main.c` | 闯关报名处 | 五关总调度（`run_exploit`） |
| `src/targets/duchamp/slide.c` | 第 1 关：偷看排班表 | KASLR 泄漏（waiter/owner/consumer 编排） |
| `src/util.c` | 魔法书包工坊 | KernelSnitch 页回收 + 假结构体喷射 |
| `src/fops.c` | 第 2 关：偷换钥匙牌 | ashmem fops 劫持 → 任意写 |
| `src/pipe.c` | 第 3 关：万能钥匙 | pipe_buffer 伪造 → 任意物理读写 |
| `src/root.c` | 第 4 关：戴园长徽章 | 花名册翻档案改 cred + 关 seccomp/SELinux |
| `src/preload.c` | 第 5 关：安插管家 | 嵌入 ksud 落盘并启动守护进程 |
| `src/ksud_blob.S` | 小机器人本体 | 编译期嵌入的 ksud 二进制 |

---

## 🗺️ 术语速查

| 术语 | 比喻 |
|---|---|
| KASLR / `_stext` | 园长办公室门牌号 |
| `rt_mutex_waiter` | 排队标签 |
| UAF（use-after-free） | 标签残留（人走了标签还在） |
| `pi_blocked_on` | 标签上写的"我在等谁" |
| EDEADLK 死锁环 | 两个小朋友面对面互相等对方 |
| `rb_erase` / `rb_insert_color` | 摘标签 / 补挂标签（要核对邻居和祖父的颜色） |
| 黑标签 / 红标签 | 摘黑标签要找邻居核对（危险）；摘红叶子零副作用（安全） |
| 线性映射基址随机化 | 街道门牌整体重排（编译期算的地址全作废） |
| 页内自包含 | 所有假道具的指针只指向书包内部，绝不越界 |
| fd_set 栈覆盖 | 把假标签逐格写进传话纸条 |
| `FUTEX_CMP_REQUEUE_PI` | 喊"换玩具！"（触发 buggy 回滚的开关） |
| `sched_setattr` nice | 喊老师"给 waiter 改优先级"（逼园长走 PI 链） |
| KernelSnitch | 数座位侧信道（摸清哪个座位连号） |
| skb 喷射 | 把书包内容喷进刚空出的座位 |
| cred | 人事档案（身份+权限印章） |
| seccomp / SELinux | 安全检查员 / 纪律监督员 |
| ksud | 藏在书包里的小管家机器人 |
| 内核 panic | 全园警报（手机重启） |

---

## 📍 当前进度（截至本文档撰写）

```
第 1 关  偷看排班表   ⚠️ 4/5 小步骤完成
  ├─ ✅ 死锁环编排（EDEADLK 触发）
  ├─ ✅ pselect 栈覆盖假标签
  ├─ ✅ KernelSnitch 页回收
  ├─ ⚠️ sched_setattr PI 链遍历 —— rbtree 染色修复已编译，待上机验证
  └─ ⏸️ boot_id 读数 → 真 _stext
第 2 关  偷换钥匙牌   ⏸️ 代码就绪，等第 1 关
第 3 关  万能钥匙     ⏸️ 代码就绪，等第 2 关
第 4 关  戴园长徽章   ⏸️ 代码就绪，等第 3 关
第 5 关  安插管家     ⏸️ 代码就绪，等第 4 关
```

**下一步动作**：设备连上 USB 后
```
adb push build/duchamp/bin/preload.so /data/local/tmp/
adb shell "LD_PRELOAD=/data/local/tmp/preload.so /system/bin/true"
```
看 `NICE-ENTER → NICE-DONE` 是否顺利打印（不重启 = 鬼标签染色修复生效）。
